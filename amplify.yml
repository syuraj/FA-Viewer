version: 1
frontend:
  phases:
    preBuild:
      commands:
        - yarn install
    build:
      commands: yarn run rw build
  artifacts:
    baseDirectory: /web/dist/
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
