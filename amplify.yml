version: 1
frontend:
  phases:
    preBuild:
      commands:
        - yarn install
    build:
      commands: yarn run rw build
  artifacts:
    baseDirectory: /
    files:
      - 'web/dist/*'
      - 'api/dist/*'
  cache:
    paths:
      - node_modules/**/*
